<main>
  <div class="container">
    <div class="row no-gutters">
      <div class="col-md-5 col-sm-10 mx-2 mx-sm-auto text-center shadow-lg rounded px-0 my-5">
        <h5 class="text-dark bg-light py-4 mb-0">ユーザ新規登録</h5>
        <div class="form-body p-4">
          <div class="mx-auto text-center">
            <p>ユーザ名・パスワードを作成してください。<br>メールアドレスはECCSのみ有効です。</p>
            <p id="domain"><%= flash[:notice] %></p>
          </div>
          <%= form_with model: @user, url: user_registration_path, id: 'new_user', class: 'new_user', local: true do |f| %>
            <%= render "devise/shared/error_messages", resource: resource %>
            <div class="input-group mb-3">
              <div class="input-group-prepend">
                <span class="input-group-text" aria-hidden="true"><i class="fas fa-user"></i></span>
              </div>
              <%= f.text_field :name, autofocus: true, class:"form-control", id:"inputName", placeholder:"ユーザネーム"%>
            </div>
            <div class="input-group mb-3">
              <div class="input-group-prepend">
                <span class="input-group-text" aria-hidden="true"><i class="fas fa-envelope"></i></span>
              </div>
              <%= f.email_field :email, autofocus: true, class:"form-control", id:"inputEmail", placeholder:"メールアドレス", :value => "@g.ecc.u-tokyo.ac.jp" %>
            </div>
            <div class="input-group mb-3">
              <div class="input-group-prepend">
                <span class="input-group-text" aria-hidden="true"><i class="fas fa-lock"></i></span>
              </div>
              <%= f.password_field :password, autocomplete: "new-password", class:"form-control", id:"inputPassword", placeholder:"パスワード" %>
              <% if @minimum_password_length %>
                <small class="form-text text-muted">(<%= @minimum_password_length %> 文字以上)</small>
              <% end %>
            </div>
            <div class="input-group mb-3">
              <div class="input-group-prepend">
                <span class="input-group-text" aria-hidden="true"><i class="fas fa-check-circle"></i></span>
              </div>
              <%= f.password_field :password_confirmation, autocomplete: "new-password", class:"form-control", id:"inputConfirmPassword", placeholder:"パスワード確認" %>
            </div>
            <p><small class="form-text text-muted">パスワードは再設定が行えませんので管理にご注意ください。</small></p>
            <%= f.submit "サインアップ", class:"btn btn-block btn-primary mt-5" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</main>